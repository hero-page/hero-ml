Generate exactly {{number_of_colors}} colors

Only return the colors, don't be conversational.

->>>>

For each of these {{number_of_colors}} colors, 
generate 5 people's names, 
where each last name is one of the above colors, respectively.

Only return the names, don't be conversational.