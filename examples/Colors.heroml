Generate a list of only {{number_of_colors}} color(s), but don't pick {{bad_color}}

Make sure it's only {{number_of_colors}} color(s)

--return-json-array-strings

->>>>

Generate a list of {{number_of_colors}} names, whose last names are respectively: {{step_1}}

